{
  "name": "@backit/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Soroban smart contracts for BACKit prediction market",
  "scripts": {
    "build": "stellar contract build",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "format": "cargo fmt",
    "clean": "cargo clean"
  }
}
